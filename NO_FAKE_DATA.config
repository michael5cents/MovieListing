STRICT_NO_FAKE_DATA_POLICY=TRUE

RULES:
- NEVER include placeholder data
- NEVER include template data  
- NEVER include assumed data
- NEVER include typical programming
- NEVER fill in missing time slots with guesses
- ONLY show data that is 100% verified from official sources
- If no verified data exists for a time slot, show NOTHING
- Empty schedules are better than fake schedules
- User explicitly demands ZERO fake data tolerance

VIOLATION_CONSEQUENCE: Complete rejection of output
VERIFICATION_REQUIRED: Every single program must be scraped from official source
FAKE_DATA_TOLERANCE: ZERO